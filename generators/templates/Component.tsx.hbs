import * as $ from './styles'

const {{pascalCase name}} = ({
  placeholder,
  ...restProps
}: {{pascalCase name}}Props & React.HTMLAttributes<HTML{{pascalCase name}}Element> & MotionProps) => {
  return <$.{{pascalCase name}}Style placeholder={placeholder} {...restProps} />
}

export interface {{pascalCase name}}Props {
  placeholder: string
}

export default {{pascalCase name}}
